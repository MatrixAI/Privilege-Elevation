# acquire PKEXEC_PATH and MECHANISM_NAME from configure
# export them to the sub-Makefiles in SUBDIRs
export pkexec_path = @PKEXEC_PATH@
export mechanism_path = $(pkglibexecdir)/@MECHANISM_NAME@

SUBDIRS = src policy